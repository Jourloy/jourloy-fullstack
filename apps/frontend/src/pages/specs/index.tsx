import { Card } from "@mantine/core";

export default function SpecsIndex() {
	return (
		<main
			className={`flex flex-col justify-center items-center min-h-[1000px] p-5 space-y-5`}
		>
			<div className={`max-w-[850px] w-full grid grid-cols-12`}>
				<Card withBorder className={`col-span-6`}>
					
				</Card>
			</div>
		</main>
	);
}
